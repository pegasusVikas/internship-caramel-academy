import React from 'react'
import {Redirect} from 'react-router-dom'
import JWT from 'jsonwebtoken'

const TypeRedirector = ({token}) => {
    
    return (
        <div>
            
        </div>
    )
}

export default TypeRedirector
